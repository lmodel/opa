# AUTOGENERATED by linkml/scripts/gen_linkml.py from upstream OPA sources.
# Do not edit by hand; rerun `just gen-linkml`.
id: https://w3id.org/lmodel/opa/authorization_input
name: authorization_input
title: Authorization Input
description: 'Input document accepted by OPA''s bundled HTTP API Authorization policy.
  Source: ``v1/schemas/authorizationPolicy.json``.'
license: Apache-2.0
version: 0.1.0

prefixes:
  openpolicyagent: https://w3id.org/lmodel/opa/
  linkml: https://w3id.org/linkml/
  schema: http://schema.org/
  dcterms: http://purl.org/dc/terms/
  rdfs: http://www.w3.org/2000/01/rdf-schema#
  d3fend: https://w3id.org/lmodel/d3fend/
  dpv: https://w3id.org/lmodel/dpv/
  iso27001: https://w3id.org/lmodel/iso27001/
  oscal: https://w3id.org/lmodel/oscal/
default_prefix: openpolicyagent
default_range: string

imports:
  - linkml:types
  - ./common

subsets:
  authorization_input_core_elements:
    description: Primary document/root entities for this module.
  authorization_input_supporting_elements:
    description: Supporting structural or helper entities.

source:
  https://github.com/open-policy-agent/opa/blob/main/v1/schemas/authorizationPolicy.json

see_also:
  - https://www.openpolicyagent.org/docs/management-decision-logs/

classes:
  AuthorizationInput:
    description: Input document the example HTTP API Authorization policy
      receives. Mirrors the OPA-provided JSON Schema at
      ``v1/schemas/authorizationPolicy.json``.
    tree_root: true
    attributes:
      identity:
        description: Caller identity, typically extracted from an
          authentication token.
        range: string
        required: true
      client_certificates:
        description: Client certificates presented during the TLS handshake.
          The example schema leaves the item shape opaque (free-form objects).
        range: Any
        multivalued: true
        inlined: true
        required: true
      method:
        description: HTTP method of the request being authorized (e.g.
          ``GET``, ``POST``).
        range: string
        required: true
      path:
        description: Request URL path split on ``/`` and decoded into a list
          of path segments.
        range: string
        multivalued: true
        required: true
      params:
        description: Query-string parameters as a free-form object.
        range: Any
        inlined: true
        required: true
      headers:
        description: Request headers as a free-form object (case insensitive
          in HTTP).
        range: Any
        inlined: true
        required: true
      body:
        description: Decoded request body as a free-form object.
        range: Any
        inlined: true
        required: true
    in_subset:
      - authorization_input_core_elements
    close_mappings:
      - d3fend:Authorization
      - dpv:AuthorisationProcedure
    related_mappings:
      - d3fend:AccessControlList
      - d3fend:AuthorizationService
      - dpv:Access
      - iso27001:SecurityControl
      - oscal:AuthorizationBoundary
