# AUTOGENERATED by linkml/scripts/gen_linkml.py from upstream OPA sources.
# Do not edit by hand; rerun `just gen-linkml`.
id: https://w3id.org/lmodel/opa/version_index
name: version_index
title: Version Index
description: 'OPA release in which each built-in, keyword, and feature was first
  introduced. Source: ``v1/ast/version_index.json``.'
license: Apache-2.0
version: 0.1.0

prefixes:
  openpolicyagent: https://w3id.org/lmodel/opa/
  linkml: https://w3id.org/linkml/
  schema: http://schema.org/
  dcterms: http://purl.org/dc/terms/
  rdfs: http://www.w3.org/2000/01/rdf-schema#
default_prefix: openpolicyagent
default_range: string

imports:
  - linkml:types

subsets:
  version_index_core_elements:
    description: Primary document/root entities for this module.
  version_index_supporting_elements:
    description: Supporting structural or helper entities.

source:
  https://github.com/open-policy-agent/opa/blob/main/v1/ast/version_index.json

classes:
  VersionIndex:
    description: Mapping from language-element names to the OPA semantic
      version that first introduced them. Grouped into built-ins,
      future-keywords, and features.
    tree_root: true
    attributes:
      builtins:
        description: Built-in name -> introduction version.
        range: ElementVersion
        multivalued: true
        inlined: true
        inlined_as_list: false
      features:
        description: Feature flag -> introduction version.
        range: ElementVersion
        multivalued: true
        inlined: true
        inlined_as_list: false
      keywords:
        description: Future keyword -> introduction version.
        range: ElementVersion
        multivalued: true
        inlined: true
        inlined_as_list: false
    in_subset:
      - version_index_core_elements
  ElementVersion:
    description: Semantic version triple identifying when an element was
      introduced.
    attributes:
      name:
        description: Name of the language element (built-in, keyword, or
          feature).
        identifier: true
        range: string
        required: true
        exact_mappings:
          - schema:identifier
          - dcterms:identifier
      major:
        description: Major version number (source field ``Major``).
        range: integer
        required: true
        minimum_value: 0
      minor:
        description: Minor version number (source field ``Minor``).
        range: integer
        required: true
        minimum_value: 0
      patch:
        description: Patch version number (source field ``Patch``).
        range: integer
        required: true
        minimum_value: 0
    in_subset:
      - version_index_supporting_elements
