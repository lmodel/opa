# AUTOGENERATED by linkml/scripts/gen_linkml.py from upstream OPA sources.
# Do not edit by hand; rerun `just gen-linkml`.
id: https://w3id.org/lmodel/opa/bundle_manifest
name: bundle_manifest
title: Bundle Manifest
description: 'Structural model of the ``.manifest`` file that ``opa build`` emits
  inside every bundle artifact (revision, roots, Wasm resolver entries, rego version
  pins, free-form metadata). Source: ``v1/bundle/manifest.schema.json``.'
license: Apache-2.0
version: 0.1.0

prefixes:
  openpolicyagent: https://w3id.org/lmodel/opa/
  linkml: https://w3id.org/linkml/
  schema: http://schema.org/
  dcterms: http://purl.org/dc/terms/
  rdfs: http://www.w3.org/2000/01/rdf-schema#
  d3fend: https://w3id.org/lmodel/d3fend/
  dpv: https://w3id.org/lmodel/dpv/
  iso27001: https://w3id.org/lmodel/iso27001/
  oscal: https://w3id.org/lmodel/oscal/
  slsa: https://w3id.org/lmodel/slsa/
default_prefix: openpolicyagent
default_range: string

imports:
  - linkml:types
  - ./common

subsets:
  bundle_manifest_core_elements:
    description: Primary document/root entities for this module.
  bundle_manifest_supporting_elements:
    description: Supporting structural or helper entities.

source:
  https://github.com/open-policy-agent/opa/blob/main/v1/bundle/manifest.schema.json

see_also:
  - https://www.openpolicyagent.org/docs/management-bundles/

classes:
  Manifest:
    description: '``Manifest`` definition from upstream JSON Schema.'
    attributes:
      file_rego_versions:
        description: Per-file Rego language version overrides. Each record
          maps a bundle file path to ``version`` where ``0`` = Rego v0 and
          ``1`` = Rego v1.
        range: FileRegoVersion
        inlined: true
        multivalued: true
        inlined_as_list: true
      metadata:
        description: '``metadata`` field of ``Manifest`` (from upstream JSON Schema).'
        range: Any
        inlined: true
      rego_version:
        description: Rego language version pinned for files in this bundle.
          ``0`` = Rego v0 (legacy); ``1`` = Rego v1 (default since OPA 1.0).
          See ``v1/bundle/bundle.go`` for the canonical mapping.
        range: integer
        minimum_value: 0
        maximum_value: 1
      revision:
        description: '``revision`` field of ``Manifest`` (from upstream JSON Schema).'
        range: string
        required: true
      roots:
        description: '``roots`` field of ``Manifest`` (from upstream JSON Schema).'
        range: string
        multivalued: true
      wasm:
        description: '``wasm`` field of ``Manifest`` (from upstream JSON Schema).'
        range: WasmResolver
        inlined: true
        inlined_as_list: true
        multivalued: true
    tree_root: true
    in_subset:
      - bundle_manifest_core_elements
    broad_mappings:
      - d3fend:AccessControlConfiguration
      - dpv:Policy
      - iso27001:DocumentedInformation
      - oscal:ComponentDefinition
      - slsa:ResourceDescriptor
    close_mappings:
      - oscal:DefinedComponent
      - slsa:Statement
    related_mappings:
      - slsa:BuildProvenance
      - slsa:DigestSet
      - slsa:VerificationSummaryAttestation
  WasmResolver:
    description: '``WasmResolver`` definition from upstream JSON Schema.'
    attributes:
      annotations:
        description: '``annotations`` field of ``WasmResolver`` (from upstream JSON
          Schema).'
        range: Any
        multivalued: true
      entrypoint:
        description: '``entrypoint`` field of ``WasmResolver`` (from upstream JSON
          Schema).'
        range: string
      module:
        description: '``module`` field of ``WasmResolver`` (from upstream JSON Schema).'
        range: string
    in_subset:
      - bundle_manifest_supporting_elements
    related_mappings:
      - d3fend:LocalAuthorizationService
      - dpv:EnforceAccessControl
      - oscal:ImplementedControlStatement
      - oscal:ImplementedRequirement
    close_mappings:
      - slsa:ResourceDescriptor
  FileRegoVersion:
    description: Typed override record for ``Manifest.file_rego_versions``.
      Provides stronger validation than a free-form map while keeping the same
      semantics.
    attributes:
      path:
        description: Bundle-relative file path receiving a Rego version
          override.
        identifier: true
        required: true
        range: string
      version:
        description: 'Rego version for this file: ``0`` (v0) or ``1`` (v1).'
        required: true
        range: integer
        minimum_value: 0
        maximum_value: 1
    in_subset:
      - bundle_manifest_supporting_elements
