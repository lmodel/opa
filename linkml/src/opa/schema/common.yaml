# AUTOGENERATED by linkml/scripts/gen_linkml.py from upstream OPA sources.
# Do not edit by hand; rerun `just gen-linkml`.
id: https://w3id.org/lmodel/opa/common
name: common
title: Common
description: Cross-cutting definitions shared by other OPA submodules
  (currently the ``Any`` placeholder used for heterogeneous source values).
  Kept in a separate module so that re-imports from the umbrella schema do not
  produce duplicate-class merge conflicts.
license: Apache-2.0
version: 0.1.0

prefixes:
  openpolicyagent: https://w3id.org/lmodel/opa/
  linkml: https://w3id.org/linkml/
  schema: http://schema.org/
  dcterms: http://purl.org/dc/terms/
  rdfs: http://www.w3.org/2000/01/rdf-schema#
default_prefix: openpolicyagent
default_range: string

imports:
  - linkml:types

subsets:
  common_core_elements:
    description: Primary document/root entities for this module.
  common_supporting_elements:
    description: Supporting structural or helper entities.

classes:
  Any:
    description: Free-form value placeholder (scalar, list, or object). Used
      to model source slots whose representation is heterogeneous or
      intentionally open (e.g. ``TypeDecl.key``, ``AuthorizationInput.body``).
    class_uri: linkml:Any
    comments:
      - Single shared definition mapped to ``linkml:Any`` (per metamodel) to
        avoid duplicate-class collisions across imported modules.
      - 'Usage is concentrated at real polymorphism boundaries: free-form HTTP payloads,
        bundle metadata/annotations, discriminated-union payloads, and upstream-opaque
        JSON Schema slots.'
      - Usage is localized (roughly 11 sites across ~60 classes); most slots
        remain explicitly typed rather than defaulting to ``Any``.
    in_subset:
      - common_supporting_elements
